def count_in_list(elements, target):
    """
    Counts occurrences of `target` in the list `elements`.

    Args:
        elements (list): List of elements to search.
        target (any): Element to count.

    Returns:
        int: Count of occurrences of `target`.
    """
    return elements.count(target)
